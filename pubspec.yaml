name: filcnaplo_kreta_api
publish_to: "none"

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  filcnaplo:
    path: ../filcnaplo/
  http: ^0.13.3
  provider: ^5.0.0
  file_picker: ^4.0.0
  json_annotation: ^4.1.0

dev_dependencies:
  build_runner: ^2.1.2
  json_serializable: ^5.0.0
